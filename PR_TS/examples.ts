import { ApiConfig } from "./api/apiConfig";

/**
 * Recive objeto de configuración 
 * @param data Object
 * @returns un string
 */
function getUrl(apiConfig: ApiConfig) : string {
    return data.name
}


getUrl({name: "Alex"});
